# Plik: .github/workflows/run_cron.yml
name: 'Cykl Symulacji AI (Cron)'

on:
  workflow_dispatch:
  schedule:
    - cron: '0 */6 * * *' 

jobs:
  run-simulations:
    runs-on: ubuntu-latest
    steps:
      # 1. Checkout kodu
      - name: Checkout Code
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          fetch-depth: 0 

      # 2. Setup Python 3.10
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
          
      # 3. Instalacja kluczowych zależności systemowych
      - name: Install System Dependencies
        run: sudo apt-get update && sudo apt-get install -y build-essential

      # 4. Instalacja zależności Python (do katalogu użytkownika)
      - name: Install Python Dependencies (User Site)
        # Używamy --user, co powoduje instalację w lokalnym katalogu użytkownika
        run: pip install -r requirements.txt --upgrade --no-cache-dir --user
        
      # 5. 🚀 KRYTYCZNY KROK: Eksport Ścieżki PYTHONPATH
      # Musimy jawnie ustawić zmienną PYTHONPATH, aby Python znalazł pakiety użytkownika
      - name: Configure PYTHONPATH
        id: set_path
        run: |
          # Zlokalizowanie ścieżki site-packages dla użytkownika (np. ~/.local/lib/python3.10/site-packages)
          USER_SITE_PACKAGES=$(python -m site --user-site)
          
          # Ustawienie zmiennej środowiskowej GITHUB_ENV
          echo "PYTHONPATH=$USER_SITE_PACKAGES" >> $GITHUB_ENV
          echo "Ścieżka PYTHONPATH ustawiona na: $USER_SITE_PACKAGES"

      # 6. Pobranie danych NLTK (dla sentymentu VADER)
      - name: Download NLTK data
        # Teraz Python powinien znaleźć NLTK dzięki nowej ścieżce
        run: python -c "import nltk; nltk.download('vader_lexicon')"
        
      # 7. DIAGNOSTYKA I Uruchomienie Głównego Skryptu
      - name: Verify and Run Script
        # Używamy jednego bloku run, aby zagwarantować użycie ustawionej PYTHONPATH
        run: |
          echo "Weryfikacja: PYTHONPATH jest: $PYTHONPATH"
          
          # Test: Jeśli ten import się uda, problem zniknął
          python -c "import xgboost; print('XGBoost jest widoczny.')"
          
          # Uruchomienie skryptu
          python run_multiple_simulations_crypto.py
