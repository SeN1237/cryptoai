# Plik: .github/workflows/run_cron.yml
name: 'Cykl Symulacji AI (Cron)'

on:
  workflow_dispatch:
  schedule:
    - cron: '0 */6 * * *' 

jobs:
  run-simulations:
    # 🚀 KRYTYCZNA ZMIANA: Użycie gotowego obrazu Docker z zainstalowanymi pakietami ML
    runs-on: ubuntu-latest
    container: 
      image: continuumio/miniconda3:latest # Gotowy obraz z Conda/Miniconda (stabilne ML)
      
    steps:
      # 1. Checkout kodu
      - name: Checkout Code
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          fetch-depth: 0 

      # 2. Instalacja brakujących zależności (tylko te, których nie ma w Conda/Miniconda)
      - name: Install Remaining Dependencies
        # Używamy PATH do instalatora Pythona wewnątrz kontenera (/opt/conda/bin/python)
        run: |
          /opt/conda/bin/pip install -r requirements.txt --upgrade --no-cache-dir
          
      # 3. Pobranie danych NLTK (dla sentymentu VADER)
      - name: Download NLTK data
        run: /opt/conda/bin/python -c "import nltk; nltk.download('vader_lexicon')"
        
      # 4. Uruchomienie głównego skryptu symulacji
      - name: Run Multi-Simulations and Aggregate
        # Używamy interpretera Pythona wewnątrz kontenera
        run: /opt/conda/bin/python run_multiple_simulations_crypto.py
